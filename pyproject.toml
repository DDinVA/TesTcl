[project]
name = "testcl-irule-emulator"
version = "0.1.0"
description = "TMOS 17.5 iRule emulator adapter"
requires-python = ">=3.13"
dependencies = [
    "hpack==4.1.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=8.0,<9",
]
