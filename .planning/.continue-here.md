---
context: default
phase: emulator-modernization
task: 2
total_tasks: 6
status: in_progress
last_updated: 2026-08-28T22:04:29Z
---

# Blocking constraints — read before anything else

- [ ] CONSTRAINT: Python runtime isolation — all Python commands must run through `uv` with Python 3.13 or newer. The user reported crashes from local/system Python; the verified host runtime is `uv run --python 3.13` using Python 3.13.9.
- [ ] CONSTRAINT: SIP work is not green — do not claim completion until the two targeted failures below are fixed and the full verification matrix passes.

## Critical anti-patterns

| Pattern | Description | Severity | Prevention mechanism |
|---|---|---|---|
| System Python execution | Running the emulator or tests with the machine-default Python caused user-reported crashes. | blocking | Prefix Python execution with `uv run --python 3.13`; use the uv-managed environment and verify `python --version`. |
| Treating a passing structured test as complete SIP fidelity | Structured SIP behavior passes one targeted test, but raw framing/state and response termination still fail. | blocking | Run all three targeted SIP tests, then the complete host suite, Tcl suite, container smoke tests, and conformance report. |

<current_state>

The repository is `/Users/dyn76614/local_projects/silas-workspace/TesTcl`, on branch `codex/modernize-baseline`. The last committed milestone is `7d49c35 feat: emulate MQTT message events`. The worktree currently has three modified files and no untracked source files: `test/test_emulator_adapter.py`, `tools/irule-emulator.py`, and `tools/semantic-mocks.tcl`.

This is a default handoff because `.planning/` had no phase/plan artifacts to identify. The current logical work is the SIP protocol slice of the broader TMOS 17.5 emulator modernization.

The SIP implementation currently adds structured request/response packet support, six SIP lifecycle event adapters, bounded raw SIP TCP framing, raw SIP UDP decoding, mutable header and payload state, and semantic implementations for the 16 SIP catalog commands. The diff is 1,263 added lines and 6 removed lines. Documentation has not yet been updated for SIP.

Targeted verification through uv/Python 3.13.9 produced:

- `test_sip_structured_lifecycle_headers_payload_and_response_rewrite`: passed.
- `test_sip_raw_tcp_and_udp_messages_drive_request_events`: error, because `::state::sip::transport` is read before it is initialized for a raw SIP packet.
- `test_sip_respond_and_discard_stop_the_forward_lifecycle`: failure, because `SIP::respond` still emits `SIP_REQUEST_DONE` instead of terminating the forwarding lifecycle.
- Python compilation and `git diff --check`: passed.
- The test module currently contains 73 test methods; the full suite has not yet been rerun after these SIP changes.

No project servers, watchers, or emulator containers are running as part of this paused work. No fork, push, or pull request has been performed.

</current_state>

<completed_work>

- Catalog consumption is wired to the pinned `tcl-lsp` source, with generated fallback coverage and semantic overrides for high-value commands.
- Committed protocol slices include HTTP/TLS/UDP/DNS foundations, WebSocket collection/frame handling, and MQTT message events.
- The emulator has API/MCP entry points, packet trace support, conformance reporting, and a container build using uv/Python 3.13.
- SIP catalog/event inventory was completed for the 17.5 target.
- SIP structured and raw adapter code plus permanent regression tests were added as WIP.

</completed_work>

<remaining_work>

- Fix SIP state initialization for raw TCP/UDP traces.
- Fix `SIP::respond` lifecycle termination so response/discard paths do not emit forwarding SEND/DONE events.
- Rerun the three SIP tests and the complete 73-test host suite through uv/Python 3.13.
- Run `./tests.sh tclsh`, container build/smoke, conformance, shell/Python syntax checks, and the mandatory self-review.
- Update `/Users/dyn76614/local_projects/silas-workspace/TesTcl/docs/emulator.md` and `docs/compatibility.md` for SIP.
- Commit the verified SIP slice with the required GitHub privacy email. Do not push without authorization.
- Continue the broader emulator roadmap: deeper semantic command coverage, actual persistence/routing behavior, richer protocol state, packet replay fidelity, and a portable user-facing workflow.

</remaining_work>

<decisions_made>

- Compatibility target is TMOS/iRules 17.5 only.
- The pinned `tcl-lsp` catalog is the source of truth for the full command/event inventory; generated stubs handle breadth while handwritten adapters provide fidelity.
- Protocol work uses both structured packet schemas and bounded raw packet decoding so the emulator stays lightweight and portable.
- SIP `persist` is currently only basic semantic state/mode behavior; it is not a real MRF persistence table or routing engine and must not be marketed as one.
- Python work is isolated in uv with Python 3.13+.

</decisions_made>

## Required reading

1. `/Users/dyn76614/local_projects/silas-workspace/TesTcl/docs/emulator.md` — current API/MCP and packet emulator behavior.
2. `/Users/dyn76614/local_projects/silas-workspace/TesTcl/docs/compatibility.md` — current compatibility claims that need SIP updates.
3. `/Users/dyn76614/local_projects/silas-workspace/TesTcl/tools/irule-emulator.py` — packet normalization, lifecycle dispatch, and conformance implementation.
4. `/Users/dyn76614/local_projects/silas-workspace/TesTcl/tools/semantic-mocks.tcl` — Tcl semantic state and SIP command handlers.
5. `/tmp/tcl-lsp` — pinned catalog/runtime source used by the adapter, if it is still available.

## Infrastructure state

- Host: uv-managed Python 3.13.9 verified with `uv run --python 3.13 python --version`.
- Container target: `testcl-irule-emulator:17.5`; the prior MQTT milestone had a Python 3.13.15 image, but the image has not been rebuilt for the current SIP work.
- External GitHub state: unchanged; no fork/push/PR.
- Background processes: none started for this work.

## Pre-execution critique required

- Design artifact: the current uncommitted SIP adapter implementation in `tools/irule-emulator.py` and `tools/semantic-mocks.tcl`.
- Critique focus: event ordering for respond/discard, initialization and reset of mutable SIP state, Content-Length framing for split/coalesced TCP messages, duplicate/compact header handling, and preservation of binary payload bytes.
- Gate: fix the two observed failures and rerun the targeted tests before expanding the SIP implementation or declaring the slice complete.

<context>

The direction remains a lightweight, containerized iRule test/emulation environment rather than a full BIG-IP replacement. The high-value strategy is to consume the entire 17.5 catalog for breadth, then progressively replace generated behavior with protocol/state semantics that matter for real iRule validation. The SIP slice is the next concrete step, but it is deliberately paused mid-implementation with known failures captured above.

</context>

<next_action>

Start with:

`rtk env TCL_LSP_ROOT=/tmp/tcl-lsp uv run --python 3.13 python -m unittest -v test.test_emulator_adapter.EmulatorAdapterTests.test_sip_structured_lifecycle_headers_payload_and_response_rewrite test.test_emulator_adapter.EmulatorAdapterTests.test_sip_raw_tcp_and_udp_messages_drive_request_events test.test_emulator_adapter.EmulatorAdapterTests.test_sip_respond_and_discard_stop_the_forward_lifecycle`

Then inspect the SIP state initialization and the `responded` branch in `tools/irule-emulator.py` before making further changes.

</next_action>
