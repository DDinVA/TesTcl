---
context: default
phase: none
task: event and protocol adapter expansion
total_tasks: ongoing
status: paused
last_updated: 2026-08-28T20:44:36Z
---

# Current state

Work is paused in `/Users/dyn76614/local_projects/silas-workspace/TesTcl` on
branch `codex/modernize-baseline`. The latest product commit is `544457e
feat: reconcile HTTP event adapter coverage`. The current WebSocket adapter
slice is intentionally uncommitted and remains in the working tree; this
handoff is the pause checkpoint.

This is a default handoff: the repository has no pre-existing `.planning`
phase or formal plan. The broad objective remains active: evolve the old
TesTcl project into a portable, containerized, TMOS 17.5-focused iRule
emulator with API/MCP access and progressively stronger behavioral fidelity.

## Completed work

- Pinned the emulator to TMOS 17.5 and the tcl-lsp registry checkout
  `cad24955f16953c2443902efd83d9f7f95d9b648`.
- Added bounded catalog inspection/chunking, static rule fidelity analysis,
  runtime-status reporting, API endpoints, persistent sessions, PCAP replay,
  and an MCP stdio facade.
- Added adapter-owned semantics for high-value Tcl/iRule areas including
  HTTP lifecycle and headers/cookies, request numbering, keep-alive,
  redirects/response commitment, raw HTTP messages, HTTP retry/close,
  collection/data/release events, load-balancer selection/failure/reselection,
  TCP collection/release/respond/close, TLS packet events, DNS packet decoding,
  persistence, tables, data groups, HSL, STATS, URI/IP/digest helpers, and
  connection endpoint state.
- Added sequence-aware TCP reassembly with bounded out-of-order handling,
  retransmission de-duplication, exact `Content-Length`, chunked transfer
  decoding with trailers, partial body buffering, no-body status handling,
  interim `HTTP_RESPONSE_CONTINUE`, and coalesced HTTP message emission.
- Added container build support using a Python 3.13 base and a uv-created
  `/opt/venv`, with the pinned Tcl LSP checkout embedded in the image.
- Reconciled the static packet-event adapter report with the implemented HTTP
  interim/release events; the committed adapter count is now 21.
- Started a structured WebSocket packet/event adapter in the working tree,
  including upgrade packets, frame packets, WS event state, and semantic
  command wrappers. A focused smoke scenario passed, but this slice has not
  yet been committed or given its final regression/documentation pass.

## Verification evidence

- Host uv environment: `/Users/dyn76614/local_projects/silas-workspace/TesTcl/.venv/bin/python`, Python 3.13.9.
- Container runtime: `/opt/venv/bin/python`, Python 3.13.15; Tcl 8.6.13.
- Image: `testcl-irule-emulator:17.5`; container smoke scenario passed.
- Host integration suite under uv/Python 3.13 with the pinned catalog: 59 tests
  passed.
- Legacy Tcl suite: all tests successful.
- `py_compile`, `bash -n scripts/emulate-irule.sh`, and `git diff --check`: passed.
- Latest committed conformance snapshot: 1,499 catalog commands; 56
  handwritten mocks, 96 semantic mocks, 1,292 generated stubs, 55 commands
  without a runtime handler; 176 events; 21 reported packet adapters.
- The focused uncommitted WebSocket smoke covered a valid request/101 response,
  client text frame, `WS::collect`, `WS::payload`, and `WS::release`; it is not
  evidence that the full WebSocket slice is complete.

## Important decisions and constraints

- Continue with TMOS 17.5 only; do not broaden version scope without an
  explicit user request.
- All Python commands must use the repo `.venv` or the container's uv virtual
  environment. Do not invoke system Python. Use Python 3.13 minimum.
- Keep behavioral additions in `tools/semantic-mocks.tcl` and the adapter;
  do not modify the external tcl-lsp checkout.
- Generated stubs count as syntax/recognition coverage only. They are not
  evidence of TMM-equivalent behavior.
- Preserve loopback binding, input bounds, and the ban on arbitrary Tcl
  evaluation.
- GitHub work has remained local. No fork, push, PR, or upstream write has
  been performed or authorized.
- Use `rtk` command prefixes per the repository instructions. Use
  `apply_patch` for file edits and stage exact paths only.
- Before presenting a code slice as complete, perform the mandated iterative
  self-review for at least three bugs/edge cases, fix them, and rerun the
  relevant checks.

## Uncommitted WebSocket work and known gaps

The working tree contains modifications to
`tools/irule-emulator.py` and `tools/semantic-mocks.tcl` for structured
WebSocket packets. Before committing them:

1. Make `WS::payload` byte-accurate using the existing Tcl-LSP payload helpers;
   the current implementation uses character ranges and is wrong for UTF-8.
2. Add the eight WebSocket packet-event entries (`WS_REQUEST`, `WS_RESPONSE`,
   client/server frame start, client/server frame done, and client/server data)
   to `PACKET_EVENT_ADAPTERS` and test the map.
3. Add regression tests for upgrade gating, event ordering, collection length
   thresholds, disabled processing, and payload replacement.
4. Reflect `WS::frame drop` and `WS::message` decisions in packet results, or
   explicitly narrow/document the behavior if that is deferred.
5. Tighten `WS::response valid` to require the full modeled upgrade condition.
6. Document that WebSocket support is structured-packet only; no raw WebSocket
   frame decoder is present yet. Decide whether `WS::disconnect` remains a
   decision-only model or emits a modeled close frame.

The committed compatibility documentation already corrects the stale claim
about retransmission/out-of-order handling: the packet reassembler supports
bounded gaps, overlap, and retransmission de-duplication.

## Required reading before resuming

1. [`docs/emulator.md`](/Users/dyn76614/local_projects/silas-workspace/TesTcl/docs/emulator.md) — public contract and supported workflow.
2. [`tools/irule-emulator.py`](/Users/dyn76614/local_projects/silas-workspace/TesTcl/tools/irule-emulator.py) — API, session, packet, and orchestration logic.
3. [`tools/semantic-mocks.tcl`](/Users/dyn76614/local_projects/silas-workspace/TesTcl/tools/semantic-mocks.tcl) — adapter-owned Tcl semantics and wrappers.
4. [`test/test_emulator_adapter.py`](/Users/dyn76614/local_projects/silas-workspace/TesTcl/test/test_emulator_adapter.py) — integration and API regression coverage.
5. [`Dockerfile.emulator`](/Users/dyn76614/local_projects/silas-workspace/TesTcl/Dockerfile.emulator) — reproducible Python/Tcl-LSP runtime.

## Remaining queue

The next concrete implementation slice is reconciling static packet-event
adapter reporting with the implemented HTTP interim/release paths. After that,
add packet-backed adapters for the next high-value protocol/event families,
expand the semantic command overlay beyond the current high-value set, and
improve the portable container/API/MCP workflow.

Do not treat the current 59-test suite or conformance counts as proof of a
true vLab. They prove the implemented slice only.

## Resume command

Start with:

```sh
cd /Users/dyn76614/local_projects/silas-workspace/TesTcl
rtk git status --short
rtk git log -3 --oneline
rtk rg -n -C 4 "websocket|WS::payload|PACKET_EVENT_ADAPTERS" tools/irule-emulator.py tools/semantic-mocks.tcl test/test_emulator_adapter.py
```

Then use the repo-local `.venv/bin/python` or `uv run --python 3.13` for host
checks, and rebuild/run `testcl-irule-emulator:17.5` for container verification.
